# M&NEM

Install:
--------

Open R and input:

```r
install.packages("devtools")

library(devtools)

install_bitbucket("cbg-ethz/mnem")

library(mnem)
```

See the vignette for working examples.

```r
vignette("mnem")
```
